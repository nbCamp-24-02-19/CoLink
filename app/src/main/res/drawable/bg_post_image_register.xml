<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shape 배경 -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/picture_small_bg" />
        </shape>
    </item>

    <!-- Resized Image 배경 -->
    <item
        android:drawable="@drawable/ic_camera_24dp"
        android:gravity="center"
        android:width="40dp"
        android:height="40dp" />
</layer-list>
